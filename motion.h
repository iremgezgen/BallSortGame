#pragma once


#include <utility>

using namespace std;


/*

class Motion
{
	Tube* source;
	Tube* destination;
	State* initial_state;
public:

	Motion(State* initial_state, Tube* source, Tube* destination) : initial_state(initial_state), source(source), destination(destination) {}

	Tube* getSource()
	{
		return source;
	}

	Tube* getDestination()
	{
		return destination;
	}
};
*/
